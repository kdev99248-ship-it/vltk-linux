[Local]
port=5003
bind=0.0.0.0
backend=mysql
; Offline-dev safety valve: set to 1 to accept the gateway even if peer IP /
; identity / password do not match the ServerList row. Leave 0 for faithful
; strict verification (the seeded ServerList row already matches the gateway).
relaxed_verify=0
